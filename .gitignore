# Node.js
node_modules/        # Der Ordner mit allen installierten npm-Paketen

# Build-Artefakte
dist/                # Der Ordner, den Webpack generiert (dein finaler Build)

# Logs
npm-debug.log* # NPM-Debug-Logs
yarn-debug.log* # Yarn-Debug-Logs
yarn-error.log* # Yarn-Fehler-Logs

# OS-generierte Dateien
.DS_Store            # macOS spezifische Datei
Thumbs.db            # Windows spezifische Datei
.localized           # macOS spezifische Datei

# Editor/IDE spezifische Dateien (Beispiele - füge hinzu, was du nutzt)
.vscode/             # Visual Studio Code Einstellungen (falls du sie nicht teilen möchtest)
.idea/               # IntelliJ/WebStorm spezifische Dateien
*.sublime-project
*.sublime-workspace

# Temporäre Dateien
*.tmp
*.bak
*.swp
*~.nib
*.log

# Umgebungsvariablen (sehr wichtig für Zugangsdaten!)
.env                 # Wenn du eine .env-Datei für Umgebungsvariablen nutzt
.env.* # Alle Dateien, die mit .env. beginnen (z.B. .env.local, .env.development)

# Andere
coverage/            # Wenn du Testabdeckungsberichte generierst
node_modules
